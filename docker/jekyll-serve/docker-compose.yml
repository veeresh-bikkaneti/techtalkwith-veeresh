services:
  blog:
    build:
      context: ../..
      dockerfile: docker/jekyll-serve/Dockerfile
    user: root
    ports:
      - "4000:4000"
    volumes:
      - ../..:/srv/jekyll
    environment:
      - JEKYLL_ENV=development